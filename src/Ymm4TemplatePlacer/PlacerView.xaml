<UserControl x:Class="Ymm4TemplatePlacer.PlacerView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 MinWidth="640" MinHeight="300">
 <Grid Margin="12">
  <Grid.RowDefinitions>
   <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
   <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
  </Grid.RowDefinitions>
  <DockPanel Grid.Row="0" Margin="0,0,0,8">
   <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
    <Button x:Name="RefreshButton" Content="更新" Command="{Binding RefreshCommand}" Padding="14,5" Margin="0,0,8,0" ToolTip="現在SceneのVoiceと登録Templateを読み直します。選択はクリアされます。"/>
    <Button x:Name="PlaceButton" Content="配置" Command="{Binding PlaceCommand}" Padding="20,5" Margin="0,0,16,0" ToolTip="全行を検証してから、このSceneのPlugin配置済み表情を置き換えます。"/>
   </StackPanel>
   <TextBlock Text="{Binding SceneName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
  </DockPanel>
  <StackPanel Grid.Row="1" Margin="0,0,0,8">
   <TextBlock Text="Characterに合うTemplateを選択 → 配置" FontWeight="SemiBold"/>
   <TextBlock Text="{Binding Summary}" Margin="0,3,0,0" Opacity="0.75"/>
  </StackPanel>
  <DataGrid x:Name="VoiceGrid" Grid.Row="2" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
   CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="True"
   EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.VirtualizationMode="Recycling"
   ScrollViewer.CanContentScroll="True" SelectionUnit="FullRow" SelectionMode="Single"
   RowHeight="36" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
   <DataGrid.Columns>
    <DataGridTextColumn Header="No" Binding="{Binding No}" IsReadOnly="True" Width="42"/>
    <DataGridTextColumn Header="Character" Binding="{Binding Character}" IsReadOnly="True" Width="100"/>
    <DataGridTemplateColumn Header="Serif" Width="*" MinWidth="120" SortMemberPath="Serif">
     <DataGridTemplateColumn.CellTemplate><DataTemplate>
      <TextBlock Text="{Binding Serif}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="5,0" ToolTip="{Binding Serif}" ToolTipService.ShowDuration="60000"/>
     </DataTemplate></DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>
    <DataGridTextColumn Header="Frame" Binding="{Binding Frame}" IsReadOnly="True" Width="65"/>
    <DataGridTextColumn Header="Length" Binding="{Binding Length}" IsReadOnly="True" Width="65"/>
    <DataGridTemplateColumn Header="Template ▼" Width="230">
     <DataGridTemplateColumn.CellTemplate><DataTemplate>
      <ComboBox ItemsSource="{Binding Choices}" DisplayMemberPath="Label" SelectedItem="{Binding SelectedChoice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
       IsEnabled="{Binding HasCandidates}" Margin="2" VerticalContentAlignment="Center" ToolTip="{Binding SelectedChoice.Label}"/>
     </DataTemplate></DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>
    <DataGridTextColumn Header="状態" Binding="{Binding State}" IsReadOnly="True" Width="72"/>
   </DataGrid.Columns>
  </DataGrid>
  <StackPanel Grid.Row="3" Margin="0,8,0,0">
   <TextBlock Text="再配置はこのSceneの CWT_TPL:face 表情だけを置換します。手動Itemは保持します。" TextWrapping="Wrap" Opacity="0.75"/>
   <TextBlock Text="候補なし: YMM4で同じCharacterの表情Itemを1個だけ含むTemplateを登録し、更新してください。" TextWrapping="Wrap" Margin="0,3,0,0" Opacity="0.75"/>
   <TextBlock x:Name="StatusText" Text="{Binding Status}" TextWrapping="Wrap" Margin="0,8,0,8">
    <TextBlock.Style><Style TargetType="TextBlock"><Style.Triggers>
     <DataTrigger Binding="{Binding HasError}" Value="True"><Setter Property="FontWeight" Value="Bold"/></DataTrigger>
    </Style.Triggers></Style></TextBlock.Style>
   </TextBlock>
  </StackPanel>
  <StackPanel Grid.Row="4" Orientation="Horizontal">
   <Button Content="Excelへ出力" Command="{Binding ExportCommand}" Padding="10,4" Margin="0,0,8,0"/>
   <Button Content="Excelから読み込み" Command="{Binding ImportCommand}" Padding="10,4"/>
   <TextBlock Text="任意: 大量の割り当て・AI連携用" Margin="12,0,0,0" VerticalAlignment="Center" Opacity="0.65"/>
  </StackPanel>
 </Grid>
</UserControl>
