<UserControl x:Class="Ymm4TemplatePlacer.PlacementBehaviorPreview"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:Ymm4TemplatePlacer"
 Focusable="False"
 IsHitTestVisible="False">
 <Border x:Name="PreviewRoot"
         BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
         BorderThickness="1"
         Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
         CornerRadius="4"
         Padding="8">
  <StackPanel>
   <TextBlock Text="配置プレビュー" FontWeight="SemiBold" Margin="0,0,0,6"/>
   <Grid>
    <Grid.ColumnDefinitions>
     <ColumnDefinition Width="*"/>
     <ColumnDefinition Width="Auto"/>
     <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Border Grid.Column="0"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            BorderThickness="1"
            CornerRadius="3"
            Padding="6"
            Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
     <StackPanel>
      <TextBlock Text="基準" FontSize="10" Opacity="0.65"/>
      <TextBlock x:Name="ContextText" Text="{Binding ContextLabel}" FontWeight="SemiBold" TextWrapping="Wrap"/>
      <TextBlock x:Name="AnchorText" Text="{Binding AnchorLabel}" FontSize="11" Opacity="0.78" TextWrapping="Wrap"/>
     </StackPanel>
    </Border>
    <TextBlock Grid.Column="1" Text="→" FontSize="18" Margin="8,0" VerticalAlignment="Center"/>
    <Border Grid.Column="2"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            BorderThickness="1"
            CornerRadius="3"
            Padding="6"
            Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
     <StackPanel>
      <TextBlock Text="置く範囲" FontSize="10" Opacity="0.65"/>
      <TextBlock x:Name="SpanText" Text="{Binding SpanLabel}" FontWeight="SemiBold" TextWrapping="Wrap"/>
      <TextBlock x:Name="AlignmentText" Text="{Binding AlignmentLabel}" FontSize="11" Opacity="0.78" TextWrapping="Wrap"/>
     </StackPanel>
    </Border>
   </Grid>
   <TextBlock x:Name="LayerText" Text="{Binding LayerLabel}" Margin="0,6,0,0" TextWrapping="Wrap"/>
   <TextBlock x:Name="FallbackText" Text="{Binding FallbackLabel}" Margin="0,2,0,0" TextWrapping="Wrap" Opacity="0.78">
    <TextBlock.Style>
     <Style TargetType="TextBlock">
      <Setter Property="Visibility" Value="Visible"/>
      <Style.Triggers>
       <DataTrigger Binding="{Binding FallbackLabel}" Value="">
        <Setter Property="Visibility" Value="Collapsed"/>
       </DataTrigger>
      </Style.Triggers>
     </Style>
    </TextBlock.Style>
   </TextBlock>
  </StackPanel>
 </Border>
</UserControl>
