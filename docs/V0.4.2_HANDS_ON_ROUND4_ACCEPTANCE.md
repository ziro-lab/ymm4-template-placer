# v0.4.2 Hands-on Round 4 — acceptance matrix

All items remain additive checkpoint/release acceptance. Earlier gates remain mandatory at Checkpoint/Release, while ordinary Focused development runs intentionally execute only the stable core plus current Round 4 checks. See `docs/VALIDATION_STRATEGY.md`.

## A. Voice operation and nested scrolling

- A1 opening a row expression ComboBox invokes the same authoritative Voice navigation command as row click.
- A2 row click and ComboBox-open never produce duplicate navigation for one input.
- A3 ComboBox remains open/usable after navigation.
- A4 opening then cancelling the ComboBox may leave the current Voice selected without changing expression content.
- A5 keyboard-open and mouse-open have equivalent Voice navigation semantics.
- A6 successful immediate expression apply can refresh Preview only through the existing latest-wins coordinator.
- A7 no new direct View-side Preview SeekAsync route exists.
- A8 inner template-list wheel scrolls the inner list while it can move in the requested direction.
- A9 inner list at the requested edge passes further wheel movement to outer Settings.
- A10 wheel outside the inner list scrolls outer Settings.
- A11 scrollbar dragging remains functional.
- A12 nested wheel handling does not globally swallow unrelated wheel input.

## B. Compact placement surface and global presentation

- B1 normal placement surface has no permanent top `Set全体` button.
- B2 Generic context header contains a compact layer affordance on the same header block.
- B3 the old full-width Generic layer row is absent.
- B4 Generic layer left-click opens a stable popup and does not immediately close from the same click.
- B5 Enter/Esc and dirty-draft admission preserve the Round 3 Generic target semantics.
- B6 Generic free target/search direction/bounds behavior remains unchanged.
- B7 bottom-right Settings opens the current Set directly when a current Set exists.
- B8 Set-wide shape is reachable in Settings and still bulk-writes current entry shapes.
- B9 individual tile shape remains editable after Set-wide shape.
- B10 presentation controls are visually/topologically global, not nested under one selected Set.
- B11 Auto/Fixed value is identical across Set switches.
- B12 fixed column count is identical across Set switches.
- B13 position shortcut mapping is identical across Set switches.
- B14 slot shortcut executes the current tile at that physical slot after a Set switch.
- B15 no Set model stores column/shortcut metadata.
- B16 expression row-height default is 36 when missing from old settings.
- B17 expression row height accepts only 32-96.
- B18 every Voice row uses the same configured height.
- B19 Serif font size is unchanged.
- B20 larger row height enables wrapped multi-line Serif text without per-row auto-expansion.

## C. Settings auto-commit and session rollback

- C1 normal Settings footer has no `変更を保存`.
- C2 normal Settings footer has no generic `変更を破棄`.
- C3 `今回の変更を戻す` is present.
- C4 session-open baseline is immutable until the session is reset/closed.
- C5 Working draft remains separate from the open baseline.
- C6 valid edits auto-commit without a user Save action.
- C7 edit bursts are root-coalesced; no polling timer is used.
- C8 complete draft validation occurs before each persisted write.
- C9 incomplete/invalid numeric/text draft is not persisted.
- C10 invalid/incomplete draft remains visible for correction.
- C11 existing settings-store atomic write and digest guard remain authoritative.
- C12 an external/in-process settings conflict does not overwrite the other change.
- C13 successful auto-commit refreshes normal placement state without resetting the selected Settings context/Set.
- C14 successful auto-commit refreshes expression vocabulary where relevant.
- C15 `今回の変更を戻す` restores the exact session-open settings snapshot.
- C16 rollback itself uses the protected atomic settings-store path.
- C17 rollback never mutates Timeline.
- C18 rollback never mutates original YMM4 templates.
- C19 “新しい表情を取り込む” remains explicit.
- C20 expression import performed in the same Settings session is included in session rollback.
- C21 leaving Settings flushes a valid pending edit.
- C22 leaving Settings never silently discards an invalid/incomplete draft.

## D. Experimental preset capability discovery

- D1 expression source switch exposes Template and `プリセット（実験的）`.
- D2 a new Tool/session starts in Template mode.
- D3 switching source mode alone causes zero Timeline mutation.
- D4 current Template expression candidate/placement behavior remains unchanged in Template mode.
- D5 Preset mode uses current Character tachie plugin and CharacterParameter; no third-party plugin allowlist is required.
- D6 probing uses a fresh FaceParameter and never an existing Timeline FaceParameter.
- D7 direct named Preset pattern enumerates candidates when coherent.
- D8 direct named pattern requires more than an incidental “Preset” substring.
- D9 local FaceParameter property attributes may discover a YMM4 preset PropertyEditor.
- D10 discovery does not perform arbitrary loaded-assembly Preset scans.
- D11 legacy tachie PropertyEditor contract is supported on the pinned host.
- D12 modern `PropertyEditorAttribute/2 + IPropertyEditorForTachieParameterAttribute` shape is supported.
- D13 built-in AnimationTachie fixture enumerates preset choices through its own editor.
- D14 selecting the AnimationTachie fixture preset mutates the fresh FaceParameter.
- D15 built-in PSD fixture enumerates preset choices through its own editor.
- D16 selecting the PSD fixture preset mutates the fresh FaceParameter.
- D17 synthetic arbitrary expanded-state editor can mutate multiple output properties without plugin-specific downstream code.
- D18 unrelated `CompressionPreset`-style noise is not promoted.
- D19 a throwing preset editor is contained to that candidate.
- D20 detector reports Strong / Experimental / None deterministically.
- D21 session cache key contains YMM4 + plugin/face runtime identity + module MVID + Character/config identity.
- D22 no persistent learned-compatibility database is added.

## E. Preset-mode placement and association

- E1 Preset mode shows/uses the existing `ExpressionPreset` geometry settings.
- E2 preset content creation produces a fresh `TachieFaceItem`.
- E3 the fresh item uses the current row Character.
- E4 selected preset is re-applied on a fresh FaceParameter at actual placement time.
- E5 Strong candidate requires the expected post-apply state hash before commit.
- E6 span uses existing `CharacterExpressionProfile.Span`.
- E7 layer placement uses existing `LayerPlanner`.
- E8 complete preflight occurs before replacement/removal mutation.
- E9 preset-generated expression receives an exact versioned managed source tag.
- E10 preset source descriptor contains capability fingerprint + candidate identity + applied state hash.
- E11 template-source expression can be atomically replaced by preset-source expression.
- E12 preset-source expression can be atomically replaced by template-source expression.
- E13 exact managed preset expression can be removed.
- E14 copied/missing/ambiguous preset association fails closed.
- E15 candidate disappearance becomes an explicit unavailable selection.
- E16 no manual/unassociated expression is removed by proximity/text/order guessing.
- E17 immediate preset selection is one logical native Undo/trial operation.
- E18 native Undo/Redo remains authoritative.
- E19 successful preset immediate apply confirms Preview through the common Voice navigation coordinator.
- E20 protected pending Excel/import assignments block entering Preset mode.
- E21 Excel import/export remains Template-only.
- E22 legacy Resync truthfully skips experimental preset associations rather than partially reconstructing them.
- E23 one incompatible Character/plugin does not disable Template mode or other Characters.
- E24 no UI Automation, fake input, private field write or new placement engine is used.

## F. Optional Item-type ordering

- F1 Item-type custom ordering is not required for Round 4 acceptance.
- F2 if implemented, it changes display order only and preserves target semantics.
- F3 if implemented, explicit reorder mode avoids click/drag ambiguity.
- F4 if implemented, `標準順に戻す` restores the stable default order.
- F5 if implemented, new/unknown Item types append safely.

## G. Safety / regression / release

- G1 normal targeted placement remains add-only.
- G2 expression destructive behavior remains limited to exact Plugin-managed associations.
- G3 Template identity/fidelity rules remain unchanged.
- G4 Generic planner semantics remain unchanged except presentation location.
- G5 Preview/latest-wins/viewport Round 3 contracts remain green.
- G6 all historical acceptance manifests/gates remain green.
- G7 add `HANDS_ON_ROUND4=PASS` plus a unique current JSON manifest.
- G8 independent evidence consumer rejects stale/missing/duplicate/failed Round 4 evidence.
- G9 Release and Proof builds remain 0 warnings / 0 errors.
- G10 exact distribution DLL native smoke passes.
- G11 package root remains exactly `Ymm4TemplatePlacer/`.
- G12 package DLL hash equals the exact native-smoked DLL.
- G13 PR #6 / #11 / #13 / #14 / #15 / Round 4 PR remain Draft/open/unmerged.
- G14 main is not merged by this pass.

## Human gate after automation

Native green still does not equal final acceptance.

The next user hands-on pass should verify at minimum:

- Generic header feels materially less tall;
- layer popup can be opened repeatedly with ordinary left-click rhythm;
- bottom Settings opens the expected current Set;
- global Fixed columns/shortcuts feel identical when switching Sets;
- long Serif is readable at chosen row height;
- dropdown-open jumps to the correct Voice before expression selection;
- Settings edits feel immediate without a Save step;
- `今回の変更を戻す` matches the user’s mental model;
- outer/inner wheel behavior feels natural;
- built-in PSD and moving-tachie expression presets appear and apply correctly;
- at least one real third-party tachie environment is tried when available;
- an unsupported preset path fails locally and leaves Template mode usable.
