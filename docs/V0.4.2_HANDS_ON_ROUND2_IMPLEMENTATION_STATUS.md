# Hands-on Round 2 implementation checkpoints

PR #14 only; base remains PR #13 head `6873adba0497c334a81097bc61fbcadb7bd40a0a`.
Keep PR #6 / #11 / #13 / #14 Draft and unmerged. Do not change `main`.

## R2-A — native-green

Source: `2309101a15a5a16aa0a2e6df0b56c479cc85e719`.

- exact YMM4 4.55.1.1 Lite / Windows / .NET 10
- run `35381655789`, job `105718983210`
- **1,025 native assertions PASS**
- `HANDS_ON_ROUND2_A=PASS`, `hands-on-round2-input.json`
- existing native ladders and markers retained
- Release / Proof: 0 warnings / 0 errors
- exact distribution DLL smoke and verified stable-root packaging PASS
- artifact `10562626777`
- independently downloaded ZIP SHA256 `e516a1554937145e368da02dfe63e766498711bf07d23727cfd4edce2fd254f5`

New proof covers actual OS mouse/keyboard input, selected-item re-click ordering, retained selection on background click, ruler click/drag, Right-key movement, playback, unknown/programmatic input, idempotent subscriptions, hide/reopen, DataContext removal/replacement and legacy/common workspace transitions. It does not claim forced preview redraw.

Two earlier failures are retained in history: an unnecessary Resources assembly reference (removed; the resource namespace lives in the existing host assembly), and a proof-only click coordinate outside the runner's physical desktop (fixed by maximizing into the real work area and verifying the actual cursor position). No production route guessing or assertion weakening was used.

## R2-B — implemented; native verification pending at this checkpoint

The common surface now wraps the two existing backends through small presentation records. Selection shows all matching `IntentPalette` Sets, without an Intent navigation layer. Generic shows Style `PaletteDefinition` Sets and delegates to `QuickDropPlanner`. Library membership/order and old serialized Intent values remain unchanged.

A lone Set displays its name but no selector; two to four Sets use direct segments; larger counts use a picker. Generic and each targeted context remember their last selected Set independently. Context navigation never clears YMM4 selection.

New `NativeHandsOnRound2SetProof` checks Generic/targeted delegation, intrinsic duration, native Undo, stale-tile rejection, retained selection, optional Set selectors and narrow layout. Unsupported multi-item Generic entries remain unavailable instead of using a new engine.

### Explicitly superseded UI assertions

The frozen Round 2 design removes the visible Intent level. Existing R7 / R12 / relative UIUX assertions now select Sets directly and assert that the Intent control is absent. Their original placement, filtering, zero-write, Undo, dense-layout, settings and source-fidelity obligations remain. Existing manifest IDs, counts, markers and independent evidence gates remain required; only the descriptions of superseded navigation are corrected.

## Remaining phases

R2-C Set-first Settings and structured relation editor; R2-D whole-tile gestures and appearance; R2-E single-click expressions, exact-dependency dirty admission and session serial allocation; R2-F consolidated Round 2 evidence, negative fixtures and final package.

This is not the full Round 2 release and not real-user hands-on acceptance.
