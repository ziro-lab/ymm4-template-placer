# Hands-on Round 2 implementation checkpoints

PR #14 only; base remains PR #13 head `6873adba0497c334a81097bc61fbcadb7bd40a0a`.
Keep PR #6 / #11 / #13 / #14 Draft and unmerged. Do not change `main`.

## Automated implementation status

R2-A through R2-E are implemented and native-green on exact YMM4 4.55.1.1 Lite / Windows / .NET 10.

### R2-A — host correction + pointer intent router

- public localization resource for the Utility Tool group
- explicit Generic / Selection placement context
- known Item/background/ruler pointer routes only; unknown route keeps current context
- Item intent consumed after host selection notification
- CurrentFrame-only keyboard/playback/programmatic changes keep context
- lifecycle attach/detach is idempotent across hide/reopen/DataContext changes

### R2-B — unified Set/tile surface

- normal model is `context -> Set -> tile`
- targeted Sets retain `IntentPalette + IntentExecutionPlan`
- Generic Sets retain Style `PaletteDefinition + QuickDropPlanner`
- no placement geometry/planner duplication and no Generic placement engine
- one Set hides selector; small counts use direct segments; larger counts use picker
- Generic display never clears YMM4 selection

### R2-C — Set-first Settings + structured 「どう置く？」

- direct target choices including 汎用/common Item types
- no normal-path Intent selector; serialized Intent remains compatibility data
- finite structured relation selectors over the existing Anchor/Duration/Alignment/Neighbor/Fallback/Direction model
- numeric tuning is under 細かく調整
- Generic exposes only existing QuickDrop semantics
- staged Save/Discard and external-change protection remain

### R2-D — whole-tile gestures and appearance

- short left click places; WPF-threshold drag reorders without placement
- right click edits alias/color/finite shape or opens Set Settings
- Rounded / Square / Circle only
- targeted order remains IntentEntry order; Generic order remains LibraryEntryIds
- appearance/order edits do not overwrite a staged Settings draft

### R2-E — expression specialist + exact dirty scope

Verified checkpoint:

- source: `f8b9991738ed93642e01339dfa6015c5b3652e2b`
- run `35426019313`, job `105851979429`
- **1,149 native assertions PASS**
- `HANDS_ON_ROUND2_E=PASS`
- Release / Proof: **0 warnings / 0 errors**
- exact distribution DLL smoke PASS
- verified stable-root package PASS
- artifact `10579515350`
- artifact SHA256 `a40f20a75221e937f64507d6cf383b605cbd852a9a76570c774e9b170c42a0a1`

Behavior:

- normal Voice row uses single-click public CurrentFrame/selection navigation
- ComboBox interaction is not swallowed by row navigation
- immediate apply/replace/remove remains exact whole-bundle behavior
- unrelated dirty Set drafts do not block expression operations
- the exact affected Set/source draft blocks before mutation and names that Set
- immediate association serial allocation is session/root high-water state seeded from compatibility settings plus observed Timeline tags; trial-only Settings writes are gone
- batch Place is visible only while relative pending assignments actually exist
- Resync remains available under secondary maintenance UI

## R2-F — final integrated evidence/package

Implemented on the current branch and awaiting the final integrated native run after the evidence-runner repair.

The final gate adds:

- `HANDS_ON_ROUND2=PASS`
- `hands-on-round2.json` with **79 native A1-G8/H1-H4 checks**
- additive validation of every R2-A through R2-E phase manifest
- independent evidence guard extended from 21 to **28 negative rejection fixtures**
- package/provenance inclusion of the Round 2 manifest
- retained P/W/WUX/R, `TEMPLATE_FIDELITY`, `RELATIVE_UIUX`, `HANDS_ON_UX_POLISH`, Release/Proof build, exact DLL smoke and stable `.ymme` root gates

The previous R2-F attempt never launched product proof: Release/Proof builds were clean, but `RunNative.ps1` had a parser error caused by a generator-side replacement escaping bug. The runner is reconstructed from the last native-green baseline rather than weakening any assertion.

## Remaining external gate

Automated native/package completion is not real-user hands-on acceptance. The final candidate still requires a separate hands-on pass with representative real templates/PSD content and normal editing rhythm. No private forced-preview API or arbitrary reflection is introduced or claimed.
