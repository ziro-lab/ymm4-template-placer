# v0.4.2 Hands-on Round 3 — workplan

Base exactly on PR #14 HEAD:

`a553ae8c32eeb8c1f125b8c005095bbe6fc98ebb`

Do not merge/rebase historical Draft PRs during this pass.

## R3-A — tile visual cleanup + Set-wide shape

Primary files:

- `IntentPalettePanel.xaml[.cs]`
- `IntentTileAppearance.cs`
- `IntentTileEditing.cs`

Implement:

1. remove the obsolete narrow color/drag bar;
2. apply finite color visibly to the tile face while preserving readable/system text and High Contrast;
3. expose Set-wide shape from placement UI;
4. bulk shape writes current entry shape fields through the existing protected settings mutation path;
5. individual shape override remains possible afterward.

Exit:

- no dedicated drag handle remains;
- color is visible on the tile itself;
- whole-tile click/drag/right-click behavior stays unchanged;
- one Set-wide shape operation updates all visible entries atomically.

## R3-B — stable grid + position shortcuts

Primary files:

- small presentation/settings model file(s)
- `IntentPalettePanel.xaml[.cs]`
- `IntentWorkspaceViewModel.cs`
- existing input-router lifecycle / a small shortcut router if cleaner

Implement:

1. Auto layout remains the compatibility/default layout while shortcuts are off;
2. add Fixed columns;
3. fixed layout never changes column count on resize;
4. position shortcut mapping is global slot -> explicit KeyGesture, not tile -> key;
5. shortcuts operate in the YMM4 main window even when the Tool itself lacks keyboard focus;
6. shortcuts are active only on the normal placement surface and only in Fixed mode;
7. exclude text/edit/ComboBox/DataGrid/modal/context-menu input and key repeat;
8. execute through the existing `ExecuteIntentTileCommand` admission path.

Exit:

- drag reorder changes what a position shortcut executes without changing shortcut data;
- hidden/unavailable slots execute nothing and do not steal the key;
- Tool hide/reopen/DataContext transitions do not duplicate key handlers.

## R3-C — Settings simplification / legacy UI removal

Primary files:

- `IntentSettingsPanel.xaml`
- Settings navigation/view model
- `PlacerView.xaml[.cs]`
- workspace startup coordination

Implement:

1. direct-render all target Item types; remove common `その他` bucket;
2. remove normal compatibility-workspace entry/return controls;
3. preserve all legacy serialized bodies;
4. ignore old `LegacyWorkspace=true` as a normal-startup mode so users cannot be stranded in hidden UI;
5. keep developer/internal compatibility code unless deletion is independently proven safe.

Exit:

- no normal UI path enters legacy workspace;
- old settings still load/save without destructive migration;
- target types are reachable without a second "other" hierarchy.

## R3-D — Generic numeric target layer + one-direction collision behavior

Primary files:

- `LayerPlanner.cs`
- `PaletteModels.cs` / additive layer policy field
- `QuickDropPlanner.cs`
- `GenericSetSettings.cs`
- Generic placement surface header

Implement:

1. numeric target layer directly visible/editable for Generic Set;
2. occupied behavior = DoNotPlace / SearchUp / SearchDown;
3. free target wins exactly;
4. one-direction search stays inside saved bounds;
5. no opposite wrap;
6. no existing Item move/shorten;
7. old JSON without the additive field preserves legacy LayerPlanner behavior.

Exit:

- exact target placement and both directional searches are native-proven;
- failure is zero-write;
- no clicked-layer coordinate inference exists.

## R3-E — expression freshness + playback/Preview/viewport navigation

Primary files:

- `PlacerView.xaml[.cs]`
- `PlacerViewModel.cs`
- `IntentExpressionImmediate.cs` or a focused navigation partial
- small bounded host adapter for Preview seek/Timeline viewport
- transient snapshot/freshness code only where needed

Implement in this order:

### E1 remove primary manual refresh

- remove top-level シーン更新;
- change empty-state wording;
- retain an internal/secondary explicit refresh only for recovery/pending Excel cases.

### E2 Voice freshness

- track current Voice reference + CharacterName/Frame/Length/Layer/Serif signature;
- subscribe only to current Timeline/current Voice lifecycle;
- coalesce notifications;
- refresh Rows only on signature change;
- association Remark-only changes do not refresh Rows;
- pending Excel/import work is never silently discarded.

### E3 bounded Preview seek capability

Resolve only the known live Preview route and exact public `SeekAsync(int)` capability.

Do not use version-number gating, arbitrary reflection scans, fake input or FocusService.

### E4 latest-wins navigation

Row click:

1. close expression trial;
2. validate row/current Voice;
3. move CurrentFrame and Selection immediately;
4. serialize/coalesce async seek so newest row wins;
5. apply viewport-follow for the newest row only;
6. truthful degraded notice if Preview or viewport capability is missing.

### E5 viewport setting

Persist Off / WhenOutside / Always, default WhenOutside.

Exit:

- Timeline-only-vs-Seek native proof remains represented in product proof;
- real row click synchronizes next playback start to the selected Voice frame on the pinned host;
- viewport follow obeys all three modes;
- rapid row clicks finish on the newest row/frame;
- ComboBox clicks remain untouched.

## R3-F — consolidated native proof, package and user docs

Add a Round 3 proof/manifest; do not weaken earlier gates.

Required:

- all old P/W/WUX/R/R2 gates;
- `TEMPLATE_FIDELITY=PASS`;
- `RELATIVE_UIUX=PASS`;
- `HANDS_ON_UX_POLISH=PASS`;
- `HANDS_ON_ROUND2=PASS`;
- new `HANDS_ON_ROUND3=PASS`;
- Release/Proof 0 warnings / 0 errors;
- exact distribution DLL smoke;
- evidence-consumer negative tests extended, not reduced;
- stable `.ymme` root `Ymm4TemplatePlacer/`.

Only after native green:

- update `docs/USAGE.md` to the actual Round 3 UI;
- package Candidate for the next real-user hands-on pass.

## Commit discipline

Suggested checkpoints:

1. R3-A
2. R3-B
3. R3-C
4. R3-D
5. R3-E1/E2
6. R3-E3/E4/E5
7. R3-F

Do not combine Generic planner changes and async Preview navigation in one checkpoint.

If a phase requires a new placement engine, private playback state, coordinate-derived layer calculation, or OS-global hotkey, stop and re-review instead of widening scope.
