# Hands-on UX polish implementation status

## H1/H2 checkpoint

- Item-first Settings navigation: current selection / Item type -> intent -> filtered Set -> details. Set-wide management stays inside Settings. Runtime type matrix is explicitly Advanced. Navigation is not a staged edit.
- Bounded Japanese Item names use the vocabulary proven in P0; unknown types never expose CLR identifiers.
- Optional per-entry display alias and finite neutral/color labels; unchanged live Template name, Locator and placement semantics. Short-name fallback and collision-safe display disambiguation.
- Uniform near-square placement tiles, dedicated drag handles forwarding only to the root reorder command, protected persistence using the existing Entries order, pending-draft/stale-object guards. Existing Settings Up/Down fallback retained.
- Normal top-level task renamed to 配置.
- Additive native H1/H2 proof covers real WPF controls, 360px layout, routed handle/drop input, metadata save/discard, no Timeline writes and order persistence. Existing regression assertions are retained, with updated wording and explicit Advanced opening.

Static XAML XML parsing and source whitespace checks passed locally. Native build/run and exact distribution verification are pending at this checkpoint. Do not treat this as the completed hands-on pass; the partial proof emits HANDS_ON_H1_H2, not HANDS_ON_UX_POLISH.

## Remaining

H3 immediate expression replacement/navigation, H4 bounded native trial session, remaining H5 host menu integration, and H6 whole-pass evidence/package gates. Human PSD/hands-on acceptance remains a separate final gate. Keep PR #13/#11/#6 Draft; do not merge or modify main.
