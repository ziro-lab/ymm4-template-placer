# Hands-on UX polish implementation status

## Integrated implementation checkpoint

H1-H5 product implementation is complete on `work/v0.4.2-hands-on-ux-polish`.

- H1: Item-first Settings, bounded Japanese Item names, Advanced-only multi-type controls, staged Save/Discard/external-conflict safety and Timeline zero-write.
- H2: `配置` wording, entry-only alias/color metadata, deterministic short-name fallback, uniform 360px tile grid, direct drag reorder using existing `Entries` order and no accidental placement.
- H3: Voice-row double-click navigation through root coordination, immediate ComboBox apply, exact association-based restore, whole-bundle atomic replace/remove, manual/unassociated preservation and failed-replacement rollback.
- H4: bounded native `UndoRedoManager.Record()` trial session. Timeline and direct Item `PropertyChanging` close the session before unrelated edits; row/task/settings/refresh/tool lifecycle boundaries are explicit. No custom Undo stack or internal collector.
- H5: supported `DefaultGroupName = "Utilities"` host integration and user-facing Item vocabulary.

The H4 public-safe boundary probe against exact YMM4 4.55.1.1 Lite completed successfully in lab run `35321201613`, artifact `10537082215`.

Integrated main-repo baseline after the proof-isolation fix:

- head `73915c4583523b570bf6e8e0d3a067d345660764`
- run `35333871754`: SUCCESS
- exact YMM4 native functional regression: SUCCESS
- Release/Proof build: SUCCESS
- exact distribution DLL native smoke: SUCCESS
- verified package/stable `Ymm4TemplatePlacer/` root: SUCCESS
- artifact `10541529227`, SHA256 `650b9ba77fc340de7a2ddc28d8644de52a541d0a6ec0db74d9f10803c0644570`

The earlier R14 failure was test-state pollution, not a product Timeline write: the H3/H4 proof restored fixture bytes without reloading the protected store digest. The proof cleanup now calls `store.Load()` after byte restoration, and the complete retained regression passes.

## H6 final gate hardening

The final checkpoint adds independent consumption of `HANDS_ON_UX_POLISH=PASS` and `hands-on-ux-polish.json`. Missing/duplicate markers, stale/failed/incomplete/duplicate-ID hands-on evidence must be rejected by the evidence guard. Packaging must retain all historical gates, require the hands-on stages, include the hands-on manifest, preserve exact distribution DLL smoke, and keep the stable `.ymme` root.

A fresh full native/package run on the H6 gate commit is still required before calling the automated pass final.

Human PSD/real-project hands-on acceptance remains a separate final gate. PR #13, PR #11 and PR #6 stay Draft; do not merge or modify `main`.
