# v0.4.2 Hands-on Round 4 — workplan

Base exactly on Round 3 final source:

`fdc3f3e5448cdf5ce7c9498776362b1bf598c2b1`

Do not merge/rebase historical Draft PRs during this pass.

Required work is ordered to put the smallest/high-confidence UX fixes first and the experimental source last.

## R4-A — Voice operation + nested wheel correctness

Primary files:

- `PlacerView.xaml`
- `PlacerView.xaml.cs`
- `ExpressionRowNavigation.cs`
- `IntentSettingsPanel.xaml[.cs]`
- one focused nested-wheel helper if needed

Implement:

1. expression ComboBox `DropDownOpened` forwards to the exact existing row navigation command;
2. ComboBox stays interactive/open; no duplicate row-click navigation;
3. successful immediate expression mutation may requeue the same latest-wins row navigation for Preview confirmation;
4. fix nested SourceList/outer Settings MouseWheel ownership;
5. preserve current row-click, Preview seek and viewport degradation behavior.

Exit:

- row click and dropdown-open share one authoritative navigation route;
- no duplicate SeekAsync path exists;
- nested wheel behavior is edge-aware and deterministic.

## R4-B — placement compaction + global presentation + row height

Primary files:

- `IntentPalettePanel.xaml[.cs]`
- `GenericLayerTargetPanel.xaml[.cs]`
- `GenericSetSettingsPanel.xaml[.cs]`
- targeted Set Settings panel/commands
- `PalettePresentationPanel.xaml[.cs]`
- `PalettePresentationSettings.cs`
- `IntentWorkspaceViewModel.cs`
- expression DataGrid XAML

Implement:

1. move Generic layer affordance into the context header;
2. left-click opens a stable small popup using the existing Generic draft;
3. remove the permanent placement-surface `Set全体`;
4. bottom-right Settings opens the current Set directly when possible;
5. move Set-wide shape action into Settings;
6. move Presentation UI to a clearly global Settings area;
7. keep layout/columns/shortcuts globally stored exactly once;
8. add global bounded expression row height 32-96, default 36;
9. Serif cells wrap without font shrink or per-row auto-height.

Exit:

- Generic gains useful tile vertical space;
- Set switching does not change Fixed columns/shortcut map;
- the same slot shortcut remains spatially consistent across Sets;
- Voice rows can show multiple lines at larger global row height.

## R4-C — safe Settings auto-commit + “今回の変更を戻す”

Primary files:

- `IntentSettingsViewModel.cs`
- `IntentSettingsDraft.cs`
- `IntentSettingsNavigation.cs`
- `IntentSettingsPanel.xaml[.cs]`
- `PlacerSettingsStore.cs` only if a small helper is needed
- expression vocabulary refresh path

Implement:

1. remove Save/Discard as normal footer actions;
2. snapshot immutable session-open baseline;
3. keep Working draft and latest committed fingerprint separately;
4. root-coalesce edit notifications;
5. build/validate complete draft before each auto-commit;
6. leave invalid/incomplete text local and unpersisted;
7. atomically save valid state through the existing store/digest guard;
8. publish refreshed placement/expression state without resetting Settings navigation;
9. add `今回の変更を戻す`;
10. rollback the complete session settings delta, including explicit “new expression import” done inside the same session;
11. flush valid pending edits before leaving Settings; never silently discard invalid draft.

Exit:

- no user Save step is needed for a valid edit;
- external settings conflict is still fail-closed;
- one session rollback restores the exact open baseline;
- Timeline and original YMM4 templates remain untouched.

## R4-D — experimental generic expression-preset source

Implement in small internal checkpoints.

### D1 source-mode shell

Primary files:

- `PlacerView.xaml[.cs]`
- `PlacerViewModel.cs`
- `AssignmentRow.cs`
- a new focused source-mode model file

Implement:

- Template / Preset（実験的） switch;
- session starts Template;
- source switching does not mutate Timeline;
- Template mode remains byte/behavior compatible where possible;
- Preset mode shows the existing `ExpressionPresetPanel` as its geometry editor;
- protected pending Excel work blocks switching into Preset mode.

### D2 capability detector

New focused files preferred, for example:

- `ExperimentalExpressionPresetCapability.cs`
- `ExperimentalExpressionPresetEditorHost.cs`
- `ExperimentalExpressionPresetCandidate.cs`

Implement:

1. exact current tachie plugin resolution;
2. current CharacterParameter read-only context;
3. fresh FaceParameter dry-run;
4. direct named Preset pattern;
5. local property-attribute/editor pattern;
6. legacy + modern tachie PropertyEditor contracts;
7. Strong / Experimental / None scoring;
8. candidate state hashing;
9. session-local fingerprint cache;
10. per-candidate failure containment.

Do not scan all loaded assemblies for arbitrary Preset members.

### D3 bare Face content planner

Use:

- `ITachiePlugin.CreateFaceParameter()`;
- current Character;
- `TachieFaceItem`;
- `ExpressionPreset`;
- `CharacterExpressionProfile.Span`;
- `LayerPlanner`.

The only new logic is expression content creation. Keep placement/occupancy/Undo architecture shared.

### D4 exact preset association + replacement

Add a small versioned experimental preset source tag and a unified managed-expression reader boundary.

Requirements:

- template-source -> preset-source replacement is atomic;
- preset-source -> template-source replacement is atomic;
- exact removal works from either mode;
- missing/copied/ambiguous tags fail closed;
- candidate disappearance becomes explicit unavailable state;
- manual/unassociated expressions are untouched;
- native Undo/Redo and trial-session behavior remain authoritative.

Do not rewrite `IntentExpressionMutation` into a generic framework solely for aesthetics. Extract only the reader/removal seam needed by both source kinds.

### D5 degraded/secondary workflows

- Excel remains Template-only;
- experimental preset association is skipped by legacy Resync with truthful reporting;
- one broken Character/plugin candidate does not disable other Characters or Template mode;
- no persistent compatibility database.

Exit:

- built-in AnimationTachie and PSD fixtures both work through structural editor detection;
- synthetic direct-name and modern third-party editor shapes work;
- false-positive and throwing candidates fail locally;
- immediate selection creates/replaces exactly one managed preset expression with one native Undo.

## R4-E — optional Item-type ordering review

Not a release gate.

Only proceed after R4-A-D are green and only if the implementation is a small display-order-only patch.

Otherwise record FUTURE and stop.

## R4-F — native regression, evidence, package, docs

Additive native coverage must include:

- all R4 acceptance IDs;
- retained Round 3 and historical manifests;
- pinned host YMM4 4.55.1.1;
- exact Release + Proof 0 warnings/errors;
- exact distribution DLL native smoke;
- independent Round 4 evidence consumer with negative fixtures;
- package root `Ymm4TemplatePlacer/`;
- exact DLL/source/provenance hashes.

Only after the final source/native candidate is green:

- update `docs/USAGE.md`;
- build final package/source archive;
- record exact run/artifact hashes.

Do not claim arbitrary third-party preset compatibility. Keep the product PR Draft for real-user hands-on acceptance.
