# v0.4.2 Hands-on Round 3 — acceptance matrix

All items are additive to prior acceptance. Earlier gates remain mandatory.

## A. Tile appearance and Set-wide operation

- A1 obsolete narrow drag/color bar is absent.
- A2 configured finite color is visibly represented by the tile face/border, not only a tiny accessory.
- A3 text remains readable with system foreground behavior.
- A4 High Contrast uses safe system brushes.
- A5 whole-tile click still places exactly once.
- A6 whole-tile drag still reorders and places zero items.
- A7 Set-wide Rounded/Square/Circle applies to every current Set entry.
- A8 Set-wide shape is a bulk write over entry shapes; no hidden inherited shape model exists.
- A9 one tile can be changed individually after a Set-wide shape operation.
- A10 staged Settings conflict protection still blocks direct appearance writes.

## B. Fixed grid and position shortcuts

- B1 Auto layout remains available.
- B2 Fixed mode keeps an exact configured column count across Tool resize.
- B3 insufficient width never silently changes fixed column count.
- B4 shortcut identity is slot position, never LibraryEntry/IntentEntry/template identity.
- B5 tile reorder changes the tile executed by that slot without changing shortcut mapping.
- B6 no shortcut is enabled by default.
- B7 shortcut may execute while keyboard focus is elsewhere inside the YMM4 main window.
- B8 shortcut is inactive when Template Placer is hidden.
- B9 shortcut is inactive outside the normal placement surface.
- B10 shortcut requires Fixed layout.
- B11 text/edit/ComboBox/DataGrid-edit/modal/context-menu input is not stolen.
- B12 key auto-repeat does not create repeated placements.
- B13 missing/unavailable slot executes nothing and leaves the key unhandled.
- B14 admitted shortcut delegates to the same tile execution/preflight/Undo route as mouse click.
- B15 repeated lifecycle attach/detach never duplicates shortcut handling.

## C. Settings simplification and compatibility

- C1 all current target Item types are directly reachable without an "その他" navigation level.
- C2 built-in display names remain Japanese.
- C3 mixed/multi-type targeting remains under Advanced.
- C4 normal UI exposes no legacy compatibility workspace entry.
- C5 normal UI exposes no return-to-relative/legacy mode toggle.
- C6 old Library/Palette/Preset/Intent compatibility data remains load/save compatible.
- C7 saved `LegacyWorkspace=true` does not reopen a hidden legacy normal workspace.
- C8 no destructive inferred migration is added.

## D. Generic target layer

- D1 no clicked-layer Y-coordinate inference exists.
- D2 numeric target layer is reachable from the Generic placement surface.
- D3 a free target layer is used exactly.
- D4 occupied + DoNotPlace produces zero Timeline mutation.
- D5 occupied + SearchUp checks only smaller layer numbers inside bounds.
- D6 occupied + SearchDown checks only larger layer numbers inside bounds.
- D7 neither directional mode wraps to the opposite side.
- D8 existing Items are never moved/shortened.
- D9 already-planned occupancy remains respected.
- D10 invalid target/bounds stop before mutation.
- D11 old settings without the additive search field preserve old LayerPlanner meaning.

## E. Expression freshness

- E1 normal header has no シーン更新 button.
- E2 adding/removing a Voice updates Rows automatically.
- E3 Voice Frame/Length/Layer/CharacterName/Serif changes refresh the affected snapshot.
- E4 association Remark-only changes do not rebuild Rows.
- E5 unchanged Voice signature produces no rebuild.
- E6 repeated notifications are coalesced.
- E7 scene/Timeline replacement rebinds watchers with no leaks/duplicates.
- E8 entering 表情をまとめて performs a freshness check.
- E9 pending Excel/import work is never silently discarded by auto refresh.
- E10 empty-state text no longer tells the user to press シーン更新.

## F. Expression navigation / Preview playback sync / viewport

- F1 row click moves `Timeline.CurrentFrame` to the Voice start and selects that Voice.
- F2 ComboBox click is not consumed by row navigation.
- F3 on the pinned host, bounded Preview capability resolves public `SeekAsync(int)`.
- F4 row navigation invokes Preview seek to the same target frame.
- F5 real native proof shows next playback begins near the selected Voice frame after the seek path.
- F6 host with missing Preview capability still performs CurrentFrame/Selection navigation.
- F7 missing Preview capability reports a concise degraded-mode notice and does not fake input.
- F8 Off viewport mode never calls ScrollFrame for navigation.
- F9 WhenOutside calls ScrollFrame only when `ContainFrameInViewport` is false.
- F10 Always calls ScrollFrame for each admitted final navigation.
- F11 ScrollFrame does not alter the requested CurrentFrame.
- F12 missing viewport capability does not disable CurrentFrame/Selection or Preview seek.
- F13 rapid row clicks are latest-wins; final Preview seek/viewport target is the newest row.
- F14 stale async completion cannot overwrite status for a newer navigation.
- F15 no `FocusService.Focus()`, UI Automation, fake Timeline click, private playback field or frame-jiggle workaround is used.

## G. Safety / regression / release

- G1 normal targeted placement remains add-only.
- G2 expression replacement/removal remains exact-association only.
- G3 Template identity/fidelity rules remain unchanged.
- G4 native Undo/Redo remains authoritative.
- G5 staged settings/external-change guards remain intact.
- G6 YMM4 version number alone does not disable Round 3 features.
- G7 context input, Preview seek and viewport follow degrade independently by capability.
- G8 all historical acceptance manifests/gates remain green.
- G9 add `HANDS_ON_ROUND3=PASS` plus a unique current JSON manifest.
- G10 independent evidence consumer rejects missing/stale/duplicate/failed Round 3 evidence.
- G11 Release and Proof builds remain 0 warnings / 0 errors.
- G12 exact distribution DLL native smoke passes.
- G13 package root remains exactly `Ymm4TemplatePlacer/`.
- G14 package DLL hash equals the exact native-smoked DLL.
- G15 PR #6 / #11 / #13 / #14 / Round 3 PR remain Draft; main is not merged by this pass.

## Human gate after automation

Native green still does not equal final acceptance.

The next user hands-on pass must verify at minimum:

- tile color/shape readability;
- Set-wide shape flow;
- fixed columns at the user's preferred Tool widths;
- real keyboard shortcut rhythm with YMM4 focused elsewhere;
- Generic target layer speed and collision choices;
- automatic Voice-list freshness during actual editing;
- row click visibly updates Preview/PSD expression;
- Play begins from the selected Voice location;
- WhenOutside viewport follow feels natural rather than jumpy.
