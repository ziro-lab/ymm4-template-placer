# v0.4.2 Hands-on Round 4 A/B/C — completion preparation

## Scope decision

PR #16 ends before experimental expression presets.

Completed product scope:

- R4-A Voice/dropdown navigation + nested Settings wheel routing;
- R4-B compact Generic placement UI + global presentation + Voice row height;
- R4-C protected automatic Settings commits + `今回の変更を戻す`;
- validation right-sizing into Focused / Checkpoint / Release.

Experimental preset work moved to `docs/EXPERIMENTAL_PRESET_HANDOFF.md` and must start in a new main-based Draft PR after this candidate is accepted and merged.

## Frozen product checkpoint

The fully native-green A/B/C product checkpoint is:

- source `486728dd9bea7a68cfbeec36a9ae2777c2f2452b`
- tree `411f55de99941ef6214d328e08bcb3b9db8cba86`
- native run #197 `35504362301`
- job `106061546725`
- 1,428 assertions PASS / zero failures
- R4 A1-A12 PASS
- R4 B1-B20 PASS
- R4 C1-C22 PASS
- Release + Proof builds 0 warnings / 0 errors
- exact distribution DLL smoke + verified packaging PASS

Subsequent branch commits only right-size validation, update documentation or comments; they do not add preset product behavior.

## Validation workflow

Read `docs/VALIDATION_STRATEGY.md`.

- ordinary product edit -> Focused;
- tests/fixtures/workflow or feature checkpoint -> Checkpoint;
- hands-on/release candidate -> Release;
- docs-only -> no YMM4.

The complete historical test assets remain source-controlled.

## Completion steps for PR #16

1. Keep A/B/C product behavior unchanged.
2. Update `docs/USAGE.md` to the actual A/B/C UI.
3. Ensure validation infrastructure changes have a green Checkpoint.
4. Run Release validation for the exact hands-on candidate.
5. Provide the resulting .ymme to the owner.
6. Keep PR #16 Draft until real-user hands-on acceptance.
7. After acceptance, merge PR #16 to main.
8. Create a fresh preset feature branch/PR from that accepted main.

## Do not do before merge

- do not start preset product code;
- do not copy D/E acceptance back into #16;
- do not add per-plugin preset adapters;
- do not reopen the A/B/C architecture;
- do not weaken Settings conflict/digest protection;
- do not merge before owner hands-on acceptance.

## Preset resume

When explicitly resumed, read `docs/EXPERIMENTAL_PRESET_HANDOFF.md` and public Lab PR #58. No new generic preset research is required unless implementation reveals a concrete unsupported host behavior.
